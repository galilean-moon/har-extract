# har-ip-extract
har-ip-extract is a simple shell script that accepts a .HAR (HTTP Archive File) as input and utilizes jq to display URL redirects and IPs in a network request to a website.

***

This program requires jq to run. You can install jq by running the command "apt-get install jq".
